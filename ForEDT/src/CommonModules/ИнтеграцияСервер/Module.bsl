
Функция ПровестиОбмен(Параметры)
	Если ТипЗнч(Параметры) = Тип("Структура") Тогда
		
	КонецЕсли;	
	УстановитьСоединение();
КонецФункции

//добавлено для синхронизации

Процедура УстановитьСоединение()
	
КонецПроцедуры